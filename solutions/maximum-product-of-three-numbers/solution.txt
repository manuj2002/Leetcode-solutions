class Solution {
public:
    int maximumProduct(vector<int>& nums) {
        sort(nums.begin(),nums.end());
        int n=nums.size();
        int m=nums[n-1]*num[n-2]*nums[n-3];
        m=max(m,nums[0]*num[1]*nums[n-1]);
        return m;
        
    }
};